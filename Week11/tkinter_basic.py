import tkinter as tk # 1. Import

# 2. สร้างหน้าต่างหลัก
root = tk.Tk()
root.title("โปรแกรมแรกของฉัน") # ตั้งชื่อหน้าต่าง
root.geometry("400x300") # กำหนดขนาด (กว้างxสูง)

# ... (เดี๋ยวเราจะเติม Widgets ตรงนี้) ...

# 3. สั่งให้โปรแกรมทำงาน (ต้องอยู่บรรทัดสุดท้ายเสมอ)
root.mainloop()


